"use client";

/**
 * Re-exports the shared preview. Operational **Monthly (000)** uses
 * `buildOperationalMonthlyExportRows` (months as columns) — see
 * `@/lib/operational-cash-outflows-excel`.
 */
export { default } from "../../../preview/cash-outflows/page";

