"use client";

/**
 * Operational preview re-uses the shared financing preview table.
 * (This restores the full spreadsheet rows/layout.)
 */
export { default } from "../../../preview/financing/page";
