"use client";

/**
 * Component 1 wizard. Reads/writes `useFinModelStore.getState().sale` only
 * (`streamKeyFromPrefix` → `sale` under `/sale/*`).
 */
export { default } from "../../cash-outflows/page";

